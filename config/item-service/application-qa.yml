spring:
    application:
        name: item-service
    rabbitmq:
#        host: CTSC00705896501
        host: localhost
        port: 5672
        username: guest
        password: guest
server:
    port: 8087
eureka: #tells about the Eureka server details and its refresh time
  client:
    healthcheck:
      enabled: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include:
        - helath
        - refresh
		- bus-refresh
		- bus-dev
default:
   item:
       id: 1001
       name: qa item
       desc: qa desc
       price: 0.0
